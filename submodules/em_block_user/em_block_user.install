<?php
/**
 * @file
 * Install file for em_block_user.module
 */

/**
 * Implements hook_schema().
 */
function em_block_user_schema() {
  $schema = array();

  $schema['em_block_user'] = array(
    'description'       => '{em_block_user} holds data mapping which authors who cannot messages to which recipients ',
    'fields'        => array(
      'author'    => array(
        'description'   => 'ID of the author',
        'type'          => 'int',
        'not null'      => TRUE,
        'unsigned'      => TRUE,
      ),
      'recipient'    => array(
        'description'   => 'ID of the recipient',
        'type'          => 'int',
        'not null'      => TRUE,
        'unsigned'      => TRUE,
      ),
    ),
    'primary key' => array('author', 'recipient'),
  );

  return $schema;
}
function em_block_user_install() {
  drupal_install_schema('em_block_user');

}

function em_block_user_uninstall() {
  drupal_uninstall_schema('em_block_user');
}

/**
 * Add primary key to {em_block_user} table.
 */
function em_block_user_update_6000() {
  $ret = array();
  db_add_primary_key($ret, 'em_block_user', array('author', 'recipient'));
  return $ret;
}
